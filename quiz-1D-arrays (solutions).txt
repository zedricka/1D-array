1. Construct the contents of the array as a completed word in a variable called "word1" and call the function to print the contents.

var array1 = ["H", "a", "p", "p", "y"];

**Answer**
var word1 = array1[0] + array1[1] + array1[2] + array1[3] + array1[4];

console.log(word1);

2. Consider the following code segment.

var array2 = [true, 0,  , 4, "who", "is", ];

console.log(array2.length);

What is printed as a result of executing the code segment?

**Answer**
6

3. What array method iterates over a list of items?

**Answer** 
forEach()

4. Consider the following code segment.

var array3 = [4, 2, 1, 4, 5, 6];

array3.pop();
array3.push(6);
array3.pop();
array3.pop();
array3.sort();

console.log(array3);
console.log(array3.length);

What is printed as a result of executing the code segment?

**Answer** 
[1, 2, 4, 4]
4

Extra Credit [1 pts] What is the index that contains the value 0 in the previous array?

**Answer**
Does not exist